---
session_id: fix-unstyled-css
timestamp: '2026-08-28T12:56:26'
mode: fix
---
[LOG_ENTRY type=PROMPT]
Page renders unstyled (plain HTML). Check Tailwind config (globals.css @tailwind
directives, import in root layout, postcss, tailwind.config content paths) and
fix what's broken. Confirm styles apply.
[/LOG_ENTRY]

[LOG_ENTRY type=RESPONSE]
Root cause was NOT Tailwind: globals.css (@import ""tailwindcss""), root layout
import, @tailwindcss/postcss (postcss.config.mjs) and installed packages were
all correct, and the compiled CSS existed on disk (44KB, full utilities). The
real bug: src/proxy.ts (Next 16 middleware) runs on every request including the
browser's /_next/static/.../css asset URL; the unauth redirect guard sent that
request to /login, so the browser got HTML instead of CSS -> unstyled.

Fix: proxy.ts now returns next() immediately for any path starting with /_next
or /api, before the auth redirect logic. Verified via dev server: the global
CSS URL now returns Content-Type text/css 44760 bytes with .btn/.flex/
text-ink-soft/bg-pink-500 present. Middleware requires a dev server restart.
[/LOG_ENTRY]
